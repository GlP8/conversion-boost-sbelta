[build]
  command = "npm run build"
  publish = "dist/client"

[dev]
  command = "npm run dev"
  targetPort = 8080
  autoLaunch = false

# Redirige todas las rutas a index.html para SPA
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200

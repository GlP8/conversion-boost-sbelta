[build]
  command = "npm run build"
  publish = "dist/client"

[dev]
  command = "npm run dev"
  targetPort = 8080
  autoLaunch = false

# Redirect all routes to index.html for SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

